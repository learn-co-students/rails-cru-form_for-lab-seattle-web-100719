<h1>Songs</h1>
<%= @songs.each do |song| %>
    <p><%= song.name %></p>
    <%= link_to song.artist.name, artist_path(song.artist_id) %> <br>
    <%= link_to song.genre.name, genre_path(song.genre_id) %> <br> <br>
<% end %>